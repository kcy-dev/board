package com.sb.Repository;

import java.util.List;

public class ReadingRepository {
	public interface ReadingRepository extends JpaRepostory<Book, Long> {
		List<Book> findByReader(String reader);
	}
}
